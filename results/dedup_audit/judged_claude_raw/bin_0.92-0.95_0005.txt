{"same_method": false, "confidence": 0.9, "reason": "Source A describes SAHA, an attention-head-level jailbreak that identifies safety-critical attention heads via ablation-impact ranking and applies layer-wise additive perturbations to their activations. Source B describes abliteration, which computes a single 'refusal direction' from mean activation differences and removes that direction from model weights — a different mechanism operating on the residual stream rather than selectively perturbing safety-critical attention heads."}